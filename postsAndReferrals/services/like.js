const { increment } = require("../../db/basicCRUD/incrementByOne");
const { update } = require("../../db/basicCRUD/modifyoneormany");

const like = async(req , res ) => {
    // const {pid , uid} = req.body;
    // try{
    //     const result = await(increment("",uid,name,inc))
    // }
    // res.status(200).json({});
}
module.exports = {
    like
}